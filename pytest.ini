[pytest]
# (optional) explicitly tell pytest where test files live
testpaths = tests

# you can keep pythonpath here too, but conftest.py is now sufficient
pythonpath = src

# avoid recursing into your virtualenv
norecursedirs = .venv venv env